import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Text Colors
  static const Color lightTextPrimary = Color(0xFF0C0F14);
  static Color lightTextSecondary = Colors.grey[600]!;
  static const Color darkTextPrimary = Color(0xFFFFFAFA);
  static Color darkTextSecondary = Colors.grey[300]!;

  // Backgorund Colors
  static const Color dark = Color(0xFF0C0F14);
  static const Color secondaryDark = Color(0xFF25282D);
  static const Color light = Color(0xFFFFFAFA);
  static const Color secondaryLight = Color(0xFFFFFFFF);
}
