import 'package:flutter/material.dart';

class CardThemes {
  CardThemes._();

  static CardTheme lightThemeData = CardTheme(
    color: Colors.grey[200],
    elevation: 0,
  );

  static CardTheme darkThemeData = CardTheme(
    color: Colors.grey[900],
    elevation: 0,
  );
}
